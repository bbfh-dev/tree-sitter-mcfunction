==================
Comment docs
==================

#> Example of a comment title
#
# Basic contents (https://example.com/docs).
#
# @params
#   $example.key param
#       The description
#   $example2.key.path.to param
#       The description 2
#
# @returns
#   $example.pack.result return
#       The result value
#       Located inside of storage:location/over/here

------------------

(source_file
  (comment
    (greedy_string))
  (comment)
  (comment
    (greedy_string))
  (comment)
  (comment
    (greedy_string))
  (comment
    (greedy_string))
  (comment
    (greedy_string))
  (comment
    (greedy_string))
  (comment
    (greedy_string))
  (comment)
  (comment
    (greedy_string))
  (comment
    (greedy_string))
  (comment
    (greedy_string))
  (comment
    (greedy_string)))

==================
Vintage comments
==================

#~>call my_arg1 @e[type=player]
#:if debug == true
say Hello World!
#:endif

------------------

(source_file
  (comment
    (command_identifier
      (identifier))
    (greedy_string))
  (comment
    (command_identifier
      (identifier))
    (greedy_string))
  (command
    (command_identifier)
    (greedy_string))
  (comment
    (command_identifier
      (identifier))))

==================
Special commands
==================

say Hello World! :)
execute run \
	return run \
	say Hi!

------------------

(source_file
  (command
    (command_identifier)
    (greedy_string))
  (command
    (command_identifier)
    (command_keyword)
    (backslash)
    (command
      (command_identifier)
      (backslash)
      (command
        (command_identifier)
        (greedy_string)))))
